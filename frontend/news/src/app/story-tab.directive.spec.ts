import { StoryTabDirective } from './story-tab.directive';

describe('StoryTabDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryTabDirective();
    expect(directive).toBeTruthy();
  });
});
